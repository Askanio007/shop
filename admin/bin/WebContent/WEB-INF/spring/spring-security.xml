<?xml version="1.0" encoding="UTF-8"?>
<beans:beans xmlns="http://www.springframework.org/schema/security"
	xmlns:beans="http://www.springframework.org/schema/beans" 
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="http://www.springframework.org/schema/beans
	http://www.springframework.org/schema/beans/spring-beans-4.0.xsd
	http://www.springframework.org/schema/security
	http://www.springframework.org/schema/security/spring-security-4.0.xsd">


  	<http pattern="/login.jsp" security="none"/>
	<http auto-config="true"> <!--  authentication-manager-ref="authManager"> -->
	  	<intercept-url pattern="/sail/**" access="hasAnyRole('ROLE_ADMIN')" />
		<intercept-url pattern="/product/**" access="hasRole('ROLE_ADMIN')" />
		<intercept-url pattern="/buyer/**" access="hasRole('ROLE_ADMIN')" />
		<intercept-url pattern="**/index.jsp" access="hasRole('ROLE_ADMIN')" />
		<intercept-url pattern="/user/**" access="hasAnyRole('ROLE_ADMIN, ROLE_USER') and isAuthenticated()" />
		<form-login login-page="/login.jsp" 
					authentication-failure-url="/login.jsp?error=true"
					username-parameter="username" 
					password-parameter="password"
					authentication-success-handler-ref="redirecor"/>		 
		<logout logout-url="/logout" logout-success-url="/login.jsp" />
		<csrf disabled="true"/>
	</http>


<beans:bean id="redirecor"
        class="user.controller.RedirectAfterLogin" />

<authentication-manager> <!--  id="authManager"> -->
	<authentication-provider>
		<password-encoder hash="md5" />
		<jdbc-user-service data-source-ref="dataSource"
			users-by-username-query="select name, password, enable from buyer where name=?"
			authorities-by-username-query="select b.name, u.role from buyer b, user_role u where b.role_id = u.role_id and b.name =?" />
	</authentication-provider>
</authentication-manager>

</beans:beans>
